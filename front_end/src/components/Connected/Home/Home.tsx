import LocationsList from 'components/Connected/LocationsList/LocationsList';
import React from 'react';

const Home = () => {
	return (
		<div>
			<LocationsList />
		</div>
	);
};

export default Home;
